#include<bits/stdc++.h>
using namespace std;

int* InsertSort(int* Array) {
    int Length = sizeof(Array) / sizeof(Array[0]);
    for (int i = 1; i < Length; i++) {
		
    }
}

int main() {

    return 0;
}
